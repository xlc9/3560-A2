import gui.control.ControlFrame;

/**
 * Launches the admin control panel.
 */
public class Driver {
    public static void main(String[] args) {
        ControlFrame controlFrame = new ControlFrame();
        controlFrame.present();
    }
}